# MongoDB Configuration
# Required: Your MongoDB connection string
# Example: mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=your_mongodb_connection_string_here

# Optional: MongoDB database name (defaults to 'video_analysis')
MONGODB_DB_NAME=video_analysis

# Google Gemini API Configuration
# Required: Your Google Gemini API key
# Get one at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_gemini_api_key_here

# Server Configuration
# Optional: Transport type (stdio or sse, defaults to sse)
TRANSPORT_TYPE=sse

# Optional: Port for SSE transport (defaults to 3000)
PORT=3000

# Optional: Log level (verbose, debug, info, warn, error, fatal)
# Defaults to 'fatal' for minimal logging
LOG_LEVEL=info